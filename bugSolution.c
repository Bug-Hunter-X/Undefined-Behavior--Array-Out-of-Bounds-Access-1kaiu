int main() {
  int arr[5] = {1, 2, 3, 4, 5};
  // Avoid accessing beyond the array bounds
  // Accessing arr[5] would be out of bounds. 
  // A safe approach would be to check the index before accessing any element
  // or use a different data structure that dynamically manage memory
  return 0;
}